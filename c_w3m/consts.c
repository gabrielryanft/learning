#include <stdio.h>

int main (){
	// making CONSTANTS! READ-ONLY VARIABLES

	const int ONE_N = 1; 
	// it is good prectice to always make tha const uppercase.
	// ONE_N = 2; can not do. (obviously)

	printf("%d", ONE_N);

	return 0;
}
